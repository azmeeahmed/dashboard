// Basic client-side data and helpers for the Noayck static dashboard
const sample = {members:[{name:'Aisha Khan',email:'aisha@example.com',plan:'Pro',joined:'2025-10-02'},{name:'John Doe',email:'john@example.com',plan:'Basic',joined:'2025-09-15'},{name:'Maya Singh',email:'maya@example.com',plan:'Pro',joined:'2025-10-10'}],courses:[{title:'Budgeting 101',students:142},{title:'Investing Basics',students:97},{title:'Debt Management',students:58}]};
// Render members table where present
document.addEventListener('DOMContentLoaded',()=>{const mList=document.getElementById('membersList');if(mList){sample.members.forEach(m=>{const tr=document.createElement('tr');tr.innerHTML=`<td>${m.name}</td><td>${m.email}</td><td>${m.plan}</td><td>${m.joined}</td>`;mList.appendChild(tr);});}const coursesGrid=document.getElementById('coursesGrid');if(coursesGrid){sample.courses.forEach(c=>{const card=document.createElement('div');card.className='card';card.innerHTML=`<h4>${c.title}</h4><p>${c.students} students</p><a href="#">View course</a>`;coursesGrid.appendChild(card);});}const recentTable=document.getElementById('recentTable');if(recentTable){const tbody=recentTable.querySelector('tbody');sample.members.slice(0,5).forEach(m=>{const tr=document.createElement('tr');tr.innerHTML=`<td>${m.name}</td><td>${m.email}</td><td>${m.plan}</td><td>${m.joined}</td>`;tbody.appendChild(tr);});}const membersCount=document.getElementById('membersCount');if(membersCount)membersCount.textContent=sample.members.length;const coursesCount=document.getElementById('coursesCount');if(coursesCount)coursesCount.textContent=sample.courses.length;const revenueVal=document.getElementById('revenueVal');if(revenueVal)revenueVal.textContent='$'+(Math.floor(Math.random()*5000)+2000);document.getElementById('searchInput')?.addEventListener('input',(e)=>{const q=e.target.value.toLowerCase();console.log('search',q);});document.getElementById('exportData')?.addEventListener('click',()=>{const csv=[['name','email','plan','joined'],...sample.members.map(m=>[m.name,m.email,m.plan,m.joined])].map(r=>r.join(',')).join('\n');const blob=new Blob([csv],{type:'text/csv'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='members.csv';a.click();});});
