// Dashboard-specific JS to render charts and wire actions
document.addEventListener('DOMContentLoaded',()=>{const ctx=document.getElementById('revenueChart');if(ctx && window.Chart){const labels=[];const data=[];for(let i=30;i>=0;i--){labels.push(i+'d');data.push(Math.floor(Math.random()*300)+50);}new Chart(ctx,{type:'line',data:{labels, datasets:[{label:'Revenue',data,backgroundColor:'rgba(31,142,250,0.12)',borderColor:'#1f8efa',tension:0.3}]},options:{plugins:{legend:{display:false}},scales:{y:{beginAtZero:true}}}});}document.getElementById('newCourse')?.addEventListener('click',()=>{alert('Open course builder (placeholder)');});document.getElementById('emailMembers')?.addEventListener('click',()=>{alert('Open email composer (placeholder)');});});
